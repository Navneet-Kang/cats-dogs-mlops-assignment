__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.env
mlruns/
data/raw/
data/processed/
artifacts/*.png
artifacts/*.json
!artifacts/.gitkeep
# The compact model is committed so CI can build a runnable image.
# For a large CNN checkpoint, use DVC/Git-LFS and add a CI pull step.
.coverage
